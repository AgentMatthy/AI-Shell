This is a AI powered terminal shell for linux. Similar to all the vibe coding agents, this one is for making modifications to the entire system through bash commands.

The bash commands are ran in a way, so that the AI does not just run a command, and gets the output, because the commands are ran as proper, incterative commands, displaying properly, and accepting user input.
